# Electron Forge requires a hoisted node_modules layout under pnpm so Electron
# and native modules resolve correctly. Without this, `electron-forge start`
# refuses to run.
node-linker=hoisted
engine-strict=true
